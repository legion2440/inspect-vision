{
  "schema_version": 1,
  "recorded_at": "2026-08-13",
  "frontend_baseline_commit": "68614fd089db5b594cfdb3b750b818f6e008ebc7",
  "current_commit_command": "git rev-parse HEAD",
  "state": "runtime-cache-correction-pending-validation-and-qualification",
  "verified": [
    "the model manifest keeps bayespfl-general-v1 as the default and exposes only Bayes-PFL plus the steel and concrete specialists",
    "factory-defect-guard-v6-mc remains registered for historical reproducibility but is hidden after its general-model rejection",
    "tracked model-selection metadata records selected and rejected candidates and prevents a rejected registered model from being exposed",
    "the Bayes-PFL deployment checkpoint remains train_visa.pth with VisA as the auxiliary training domain and MVTec AD as the distinct held-out qualification domain",
    "Bayes-PFL category input is normalized and validated before inference",
    "Dashboard, Inspect, live stream, and Samples share explicit model/category state without automatic model switching",
    "the operator Samples page contains the intended 14-item showcase: eight MVTec Bayes examples plus three steel and three concrete specialist examples",
    "the twelve local VisA files in backend/samples/demo remain separate demo/evidence material and are not projected into the operator Samples page",
    "ordinary API validation skips the two byte-fetch checks unless INSPECT_VISION_RUN_NETWORK_TESTS=1 is set",
    "the rejected legacy general YOLO no longer contributes classes to the visible registry or history filter",
    "the model installer provides manual artifact fallback details when downloads fail",
    "runtime auto device selection prefers CUDA, then Apple MPS, then CPU",
    "the retained production-service probe at source commit 5824fa1a647e1e05597f6750a2fd43e9d51e38aa qualified all three exposed models on PyTorch 2.12.1+cu126, CUDA 12.6, and NVIDIA GeForce RTX 4080 Laptop GPU; it is historical after the guided-runtime cache correction",
    "historical runtime bundles remain bound to the source snapshots they actually executed"
  ],
  "known_limitations": [
    {
      "id": "repository-validation-pending",
      "area": "verification-evidence",
      "detail": "The corrected guided-runtime cache and restored operator sample catalog require canonical validation on the current source."
    },
    {
      "id": "runtime-requalification-pending",
      "area": "verification-evidence",
      "detail": "Detection runtime source changed after the retained CUDA evidence. A fresh production-service probe is required before current runtime qualification can be claimed."
    },
    {
      "id": "operator-showcase-network-source",
      "area": "backend-api",
      "detail": "Operator sample image bytes use revision-pinned network sources. Catalog/API structure is validated offline; byte-fetch integrity checks are opt-in."
    },
    {
      "id": "bayespfl-generic-native-output",
      "area": "defect-detection",
      "detail": "Bayes-PFL localizes anomalies but emits the native generic type anomaly rather than a semantic defect subtype."
    },
    {
      "id": "bayespfl-adapter-threshold",
      "area": "defect-detection",
      "detail": "The fixed 0.72 map threshold, component-area filter, and bbox padding are application adapter settings rather than an upstream accuracy claim."
    },
    {
      "id": "bayespfl-binary-license-scope",
      "area": "defect-detection",
      "detail": "The upstream Bayes-PFL repository states MIT licensing, but the distributed train_visa.pth checkpoint does not carry a separately stated binary license in tracked source metadata."
    },
    {
      "id": "automated-browser-coverage-partial",
      "area": "verification-evidence",
      "detail": "Python API/detection/storage tests, frontend tests, validators, and a production build are present; full browser end-to-end automation is not part of repository checks."
    }
  ]
}
