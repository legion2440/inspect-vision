{
  "schema_version": 1,
  "recorded_at": "2026-08-10",
  "frontend_baseline_commit": "68614fd089db5b594cfdb3b750b818f6e008ebc7",
  "current_commit_command": "git rev-parse HEAD",
  "state": "runtime-cache-correction-pending-validation-and-qualification",
  "verified": [
    "the model manifest keeps bayespfl-general-v1 as the default and exposes only Bayes-PFL plus the steel and concrete specialists",
    "factory-defect-guard-v6-mc remains registered for historical reproducibility but is hidden after its general-model rejection",
    "tracked model-selection metadata records selected and rejected candidates and prevents a rejected registered model from being exposed",
    "the Bayes-PFL deployment checkpoint remains train_visa.pth with VisA as the auxiliary training domain and MVTEC AD as the distinct held-out qualification domain",
    "Bayes-PFL category input is normalized to lowercase, accepts underscores for backward compatibility, and rejects malformed context before inference",
    "the public model registry exposes curated category examples with local, upstream, and comparison evidence labels",
    "Dashboard, Inspect, live stream, and Samples share model/category state without automatically switching the selected model",
    "the shared product/category control is an editable combobox that keeps custom input while exposing curated values across Dashboard, Inspect, and Samples",
    "the operator Samples page is backed by the same twelve tracked VisA images in backend/samples/demo and no longer maintains a second network-backed showcase catalog",
    "the canonical complete repository validation passed before the current cache/sample cleanup at commit b5833bb0db81d3e0ca50d4d7759d3d55b08f46bd; the current source still requires its own canonical validation record",
    "the rejected legacy general YOLO no longer contributes classes to the user-visible model registry or history filter",
    "the model installer provides a manual artifact fallback with source, destination, expected size, SHA-256, and retry command when a download fails",
    "runtime auto device selection prefers CUDA, then Apple MPS, then CPU, while shared dependency pins no longer force a CPU-only PyTorch build",
    "the retained production-service probe at source commit 5824fa1a647e1e05597f6750a2fd43e9d51e38aa qualified all three exposed models on PyTorch 2.12.1+cu126, CUDA 12.6, and NVIDIA GeForce RTX 4080 Laptop GPU; it is historical after the guided-runtime cache correction",
    "historical runtime bundles remain recoverable from Git history and are validated against their recorded source snapshots rather than rewritten to current source"
  ],
  "known_limitations": [
    {
      "id": "repository-validation-pending",
      "area": "verification-evidence",
      "detail": "The guided-runtime cache correction and local demo-catalog consolidation require the canonical complete repository validation on current source."
    },
    {
      "id": "runtime-requalification-pending",
      "area": "verification-evidence",
      "detail": "Detection runtime source changed after the retained CUDA evidence. A fresh production-service probe is required before current runtime qualification can be claimed."
    },
    {
      "id": "bayespfl-generic-native-output",
      "area": "defect-detection",
      "detail": "Bayes-PFL localizes anomalies but emits the native generic type anomaly rather than a semantic defect subtype. Supported specialists remain preferable when semantic domain-specific classes are required."
    },
    {
      "id": "bayespfl-adapter-threshold",
      "area": "defect-detection",
      "detail": "The fixed 0.72 map threshold, component-area filter, and bbox padding are application adapter settings. Upstream evaluation does not provide a fixed deployment bbox threshold, so these settings are not an upstream accuracy claim."
    },
    {
      "id": "bayespfl-binary-license-scope",
      "area": "defect-detection",
      "detail": "The upstream Bayes-PFL repository states MIT licensing, but the distributed train_visa.pth checkpoint does not carry a separately stated binary license in the tracked source metadata."
    },
    {
      "id": "automated-browser-coverage-partial",
      "area": "verification-evidence",
      "detail": "Python API/detection/storage tests, frontend utility tests, validators, and a production build are present; full browser end-to-end automation is not part of the repository checks."
    }
  ]
}
